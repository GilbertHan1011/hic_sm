name: fastqc-env
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - snakemake-minimal==7.32.4 # Using snakemake-minimal is often sufficient
  - fastqc>=0.11.9       # The primary tool for the workflow
  - python==3.10.14         # Specify a compatible Python version
  - pandas
  - numpy